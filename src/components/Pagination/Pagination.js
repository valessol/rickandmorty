import React from 'react'
import { StyledButton } from '../Cards/Card.Style'

const Pagination = ({ label }) => {
    return (
        <div>
            <StyledButton />
            <StyledButton />

        </div>
    )
}

export default Pagination
